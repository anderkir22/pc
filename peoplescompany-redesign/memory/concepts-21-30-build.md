---
name: concepts-21-30-build
description: Active task — building homepage concepts 21–30 for the PeoplesCompany redesign; where it paused
metadata:
  type: project
---

Active build phase (paused 2026-06-09): a fresh batch of 10 homepage wireframes **concepts 21–30**
that synthesize all three feedback sets, with color (PC blue + green `#005F5B`), real photos, and
the agreed must-have elements.

**Full details + resume checklist live in `CLAUDE.md` → "▶ RESUME HERE" and the brief
`wireframes/index/BRIEF-21-30.md`.** (CLAUDE.md is the running project log — read it first.)

State: green standardized on `#005F5B`; **concept-21.html built** as the canonical template;
**22–30 still to build** (use 21 as the template, can parallelize), then add 21–30 to the gallery
`wireframes/index/index.html` and push to the `pc` GitHub repo.
